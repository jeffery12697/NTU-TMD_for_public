const getDepPostName = {
    dep_id: "dep_id"
}

const newPostName = {
    dep_id: "dep_id",
    user_mail: "user_mail",
    title: "title",
    content: "content",
}

const newCommentName = {
    post_id: "post_id",
    "user_mail": "user_mail",
    "comment": "comment",
}

// export { getDepPostName, newPostName, newCommentName }